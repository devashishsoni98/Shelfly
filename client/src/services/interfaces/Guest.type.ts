export interface Guest {
    name: string,
    guestId: number,  // Replace with an actual UserProfile ID
    eventId: number, // Replace with an actual Event ID
    emails:string,
    status:string,
    guestCode:string
}